        
        
        <!-- Page Intro -->
        <div class="bg-primary bg-opacity-10 border border-primary border-opacity-25 rounded-3 p-3 mb-4 d-flex justify-content-between align-items-center">
          <div class="d-flex align-items-center">
            <i class="bi bi-speedometer2 text-primary me-2"></i>
            <span class="fw-semibold text-primary">Overview</span>
          </div>
          <div class="btn-group">
            <a href="/admin/players/add" class="btn btn-sm btn-primary"><i class="bi bi-person-plus me-1"></i>Add Player</a>
            <a href="/admin/auction" class="btn btn-sm btn-outline-primary"><i class="bi bi-hammer me-1"></i>Auction</a>
          </div>
        </div>

        <!-- Stats Cards -->
        <div class="row mb-4">
          <div class="col-xl-3 col-md-6 mb-4">
            <div class="card stat-card">
              <div class="card-header d-flex justify-content-between align-items-center bg-primary text-white">
                <h5 class="mb-0">Total Players</h5>
                <i class="bi bi-people stat-icon"></i>
              </div>
              <div class="card-body d-flex align-items-center">
                <div class="stat-number display-6 fw-bold"><%= players ? players.length : 0 %></div>
              </div>
            </div>
          </div>
          
          <div class="col-xl-3 col-md-6 mb-4">
            <div class="card stat-card">
              <div class="card-header d-flex justify-content-between align-items-center bg-primary text-white">
                <h5 class="mb-0">Sold Players</h5>
                <i class="bi bi-check-circle stat-icon"></i>
              </div>
              <div class="card-body d-flex align-items-center">
                <div class="stat-number display-6 fw-bold"><%= soldPlayers ? soldPlayers.length : 0 %></div>
              </div>
            </div>
          </div>
          
          <div class="col-xl-3 col-md-6 mb-4">
            <div class="card stat-card">
              <div class="card-header d-flex justify-content-between align-items-center bg-primary text-white">
                <h5 class="mb-0">Unsold Players</h5>
                <i class="bi bi-x-circle stat-icon"></i>
              </div>
              <div class="card-body d-flex align-items-center">
                <div class="stat-number display-6 fw-bold"><%= unsoldPlayers ? unsoldPlayers.length : 0 %></div>
              </div>
            </div>
          </div>
          
          <div class="col-xl-3 col-md-6 mb-4">
            <div class="card stat-card">
              <div class="card-header d-flex justify-content-between align-items-center bg-primary text-white">
                <h5 class="mb-0">Total Teams</h5>
                <i class="bi bi-shield-shaded stat-icon"></i>
              </div>
              <div class="card-body d-flex align-items-center">
                <div class="stat-number display-6 fw-bold"><%= teams ? teams.length : 0 %></div>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Quick Actions -->
        <div class="row mb-4">
          <div class="col-12">
            <div class="card content-card">
              <div class="card-header">
                <h5 class="mb-0">Quick Actions</h5>
              </div>
              <div class="card-body">
                <div class="row g-3">
                  <div class="col-md-3 col-sm-6">
                    <a href="/admin/players/add" class="btn btn-outline-primary w-100 d-flex align-items-center justify-content-center py-3">
                      <i class="bi bi-person-plus me-2"></i>
                      <span>Add Player</span>
                    </a>
                  </div>
                  <div class="col-md-3 col-sm-6">
                    <a href="/admin/auction" class="btn btn-outline-primary w-100 d-flex align-items-center justify-content-center py-3">
                      <i class="bi bi-hammer me-2"></i>
                      <span>Manage Auction</span>
                    </a>
                  </div>
                  <div class="col-md-3 col-sm-6">
                    <a href="/admin/teams" class="btn btn-outline-primary w-100 d-flex align-items-center justify-content-center py-3">
                      <i class="bi bi-people me-2"></i>
                      <span>Manage Teams</span>
                    </a>
                  </div>
                  <div class="col-md-3 col-sm-6">
                    <a href="/admin/players" class="btn btn-outline-primary w-100 d-flex align-items-center justify-content-center py-3">
                      <i class="bi bi-list-ul me-2"></i>
                      <span>View Players</span>
                    </a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Recent Players and Teams -->
        <div class="row align-items-stretch">
          <div class="col-lg-6 mb-4 d-flex">
            <div class="card content-card h-100 w-100">
              <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Recent Players</h5>
                <a href="/admin/players" class="btn btn-sm btn-outline-primary">View All</a>
              </div>
              <div class="card-body p-0">
                <div class="table-responsive">
                  <table class="table table-hover table-sm align-middle mb-0">
                    <thead>
                      <tr>
                        <th>Name</th>
                        <th>Role</th>
                        <th>Base Price</th>
                        <th>Status</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% if (players && players.length) { %>
                        <% players.slice(0,5).forEach(function(p){ %>
                          <tr>
                            <td><%= p.name %></td>
                            <td><%= p.role %></td>
                            <td>₹<%= (p.basePrice || 0).toLocaleString('en-IN') %></td>
                            <td>
                              <% if (p.status === 'sold') { %>
                                <span class="badge bg-success">Sold</span>
                              <% } else if (p.status === 'in-auction') { %>
                                <span class="badge bg-warning text-dark">In Auction</span>
                              <% } else { %>
                                <span class="badge bg-secondary">Unsold</span>
                              <% } %>
                            </td>
                          </tr>
                        <% }) %>
                      <% } else { %>
                        <tr>
                          <td colspan="4" class="text-center text-muted">No players found</td>
                        </tr>
                      <% } %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
          
          <div class="col-lg-6 mb-4 d-flex">
            <div class="card content-card h-100 w-100">
              <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Teams</h5>
                <a href="/admin/teams" class="btn btn-sm btn-outline-primary">View All</a>
              </div>
              <div class="card-body p-0">
                <div class="table-responsive">
                  <table class="table table-hover table-sm align-middle mb-0">
                    <thead>
                      <tr>
                        <th>Team</th>
                        <th>Owner</th>
                        <th>Budget</th>
                        <th>Players</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% if (teams && teams.length) { %>
                        <% teams.slice(0,5).forEach(function(t){ %>
                          <tr>
                            <td><%= t.name %></td>
                            <td><%= t.owner %></td>
                            <td>₹<%= (t.budget || 0).toLocaleString('en-IN') %></td>
                            <td><%= t.players ? t.players.length : 0 %></td>
                          </tr>
                        <% }) %>
                      <% } else { %>
                        <tr>
                          <td colspan="4" class="text-center text-muted">No teams found</td>
                        </tr>
                      <% } %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Toggle sidebar on mobile
      const sidebarToggle = document.getElementById('sidebarToggle');
      const sidebar = document.getElementById('sidebarMenu');
      
      if (sidebarToggle) {
        sidebarToggle.addEventListener('click', function() {
          sidebar.classList.toggle('show');
        });
      }
      
      // Animate stat numbers
      const statNumbers = document.querySelectorAll('.stat-number');
      
      const animateValue = (element, start, end, duration) => {
        let startTimestamp = null;
        const step = (timestamp) => {
          if (!startTimestamp) startTimestamp = timestamp;
          const progress = Math.min((timestamp - startTimestamp) / duration, 1);
          element.textContent = Math.floor(progress * (end - start) + start);
          if (progress < 1) {
            window.requestAnimationFrame(step);
          }
        };
        window.requestAnimationFrame(step);
      };
      
      statNumbers.forEach(stat => {
        const endValue = parseInt(stat.textContent);
        animateValue(stat, 0, endValue, 1000);
      });
    });
  </script>